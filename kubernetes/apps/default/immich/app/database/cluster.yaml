---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/postgresql.cnpg.io/cluster_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-database
  namespace: default
spec:
  instances: 3
  postgresql:
    # Both libraries are required during the pgvecto.rs -> VectorChord migration
    # (Immich >= v1.133 / v2.5.x). Drop "vectors.so" once migration completes.
    shared_preload_libraries:
      - "vchord.so"
      - "vectors.so"
    parameters:
      max_connections: "300"
      shared_buffers: 512MB
  # Transitional image carrying BOTH pgvecto.rs and VectorChord so Immich can
  # migrate existing vector data. After migration, switch to a vchord-only image:
  # ghcr.io/tensorchord/cloudnative-vectorchord:16-0.3.0
  imageName: ghcr.io/corentingiraud/cnpg-pgvector-vectorchord:16-migration
  affinity:
    nodeSelector:
      # use all the same arch
      kubernetes.io/arch: amd64
  primaryUpdateStrategy: unsupervised
  storage:
    size: 10Gi
    storageClass: openebs-hostpath
  nodeMaintenanceWindow:
    inProgress: false
    reusePVC: true
  resources:
    requests:
      cpu: 500m
    limits:
      memory: 2Gi
  enableSuperuserAccess: true
  monitoring:
    enablePodMonitor: true
    # Ref: https://github.com/cloudnative-pg/cloudnative-pg/issues/2501
    podMonitorMetricRelabelings:
      - {
          sourceLabels: ["cluster"],
          targetLabel: cnpg_cluster,
          action: replace,
        }
      - { regex: cluster, action: labeldrop }
  backup:
    retentionPolicy: 30d
    barmanObjectStore: &barmanObjectStore
      data:
        compression: bzip2
      wal:
        compression: bzip2
        maxParallel: 2
      destinationPath: s3://databases/
      endpointURL: https://s3.${SECRET_DOMAIN:=internal}
      serverName: immich-v3
      s3Credentials:
        accessKeyId:
          name: minio-immich-pgsql
          key: minio_s3_access_key
        secretAccessKey:
          name: minio-immich-pgsql
          key: minio_s3_secret_access_key
  managed:
    roles:
      - name: immich
        ensure: present
        login: true
        superuser: true
        passwordSecret:
          name: immich-pg-secret
  bootstrap:
    initdb:
      database: immich
      owner: immich
      postInitApplicationSQL:
        - CREATE EXTENSION IF NOT EXISTS "vectors";
