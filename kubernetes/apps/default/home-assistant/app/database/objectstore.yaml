---
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: home-assistant-objectstore
  namespace: default
spec:
  retentionPolicy: 30d
  configuration:
    destinationPath: s3://databases/
    endpointURL: https://s3.${SECRET_DOMAIN:=internal}
    # serverName is set via the Cluster's plugin parameters (forbidden here)
    s3Credentials:
      accessKeyId:
        name: minio-home-assistant-pgsql
        key: minio_s3_access_key
      secretAccessKey:
        name: minio-home-assistant-pgsql
        key: minio_s3_secret_access_key
    wal:
      compression: bzip2
      maxParallel: 2
    data:
      compression: bzip2
