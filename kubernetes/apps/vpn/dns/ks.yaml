---
# yaml-language-server: $schema=https://kubernetes-schemas.dmfrey.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app vpn-dns
  namespace: flux-system
spec:
  targetNamespace: vpn
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  path: "./kubernetes/apps/vpn/dns/app"
  prune: true
  sourceRef:
    kind: GitRepository
    name: home-kubernetes
  wait: true
  interval: 10m
  retryInterval: 1m
  timeout: 5m
  dependsOn:
    - name: vpn-gateway
    - name: multus-config
    - name: node-network-operator-config
  postBuild:
    substitute:
      APP: *app
    substituteFrom: []
