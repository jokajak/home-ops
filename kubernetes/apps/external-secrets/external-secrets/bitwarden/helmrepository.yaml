---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: bitwarden-eso-provider
  namespace: external-secrets
spec:
  interval: 2h
  # Chart moved: small-hack -> open-engineering, now published via the Codeberg
  # (Forgejo) Helm package registry. The old small-hack GitHub Pages repo 404s,
  # which is why Renovate silently stopped proposing updates (dead datasource).
  url: https://codeberg.org/api/packages/open-engineering/helm
